# Local development environment example — no production credentials.
# Copy to .env.development locally and fill only with an isolated development project.
VITE_INCLUDE_SEED=true
VITE_SUPABASE_URL=https://<development-project>.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<development-publishable-key>
